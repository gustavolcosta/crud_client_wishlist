class Client
{
  constructor(id, name, email, favoriteProducts)
  {
    this.id = id,
    this.name = name,
    this.email = email,
    this.favoriteProducts = favoriteProducts
  }
  
}

module.exports = Client